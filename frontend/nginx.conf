server {
  listen 80;
  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri $uri/ /index.html;
  }
  location /config.js {
    add_header Content-Type application/javascript;
    return 200 "window.appConfig = { backendUrl: '$VITE_BACKEND_URL' };";
  }
}
